---
kind: context
id: CTX-NATIVE-PRIVATE-APPROVAL-DELIVERY
title: "Original approval SDK transport custody"
status: Accepted
---

The accepted ADR112 partition starts at c527314 (e5fd184 service plus c274d11
card prerequisite). The exact28-path task parsed/linted before source edits.
Ordinary Agent routes and approval-reader purpose stay distinct. Qualify actual
fresh enrollment, encrypted owner decryption, finite custody and retained close;
do not infer service/client completion or platform execution from compilation.

The fixture starts the service SDK through its real fresh-account protocol and
uses a separate recipient OlmMachine for actual owner decryption. Public peer
anchors and recipient trust are explicit fixture provisioning; no signing seeds,
sessions or service identity are injected into the sender. The complete-history
corruption cases edit the original encrypted test journal with its test-held key
and prove protected restore refusal, not attacker possession of a production key.
The capacity case has one real accepted transmission plus63 synthetic valid
receipt metadata records in the protected journal: it qualifies the64-record
bound/no eviction, not64 separate network deliveries. The unchanged48KiB card and
60KiB encrypted room wire caps are both enforced; actual encryption expansion
past the latter refuses before a secret-bearing PUT.

The retained-close failure case uses the existing SDK CloseFault command. The
actual stores/runtime/lock are closed and an injected OutcomeUnknown result is
retained across caller loss and repeat close. This qualifies failure-result
custody, not an operating-system-induced SQLite close error. The interrupted
intake case uses the existing fault after actual receive_sync_response and the
persisted Applying phase; cancellation occurs only after that original result,
so it cannot fabricate entry to SDK mutation.

Original exploratory failures remain separate from later qualification: the cold
first crypto run compiled in40m13s and returned Cancelled during fresh enrollment;
its source was still being finalized during dependency compilation. The first
full package run had123 passing tests and one fixture failure: cancellation at
HTTP response preparation correctly returned Cancelled before SDK Apply. The
fixture now waits for the actual interrupted-apply result before cancellation.
No production deadline, retry or purpose policy changed in response.

Final local macOS qualification: complete Matrix package145/145 (124 library,
6/6/9 integration; empty doc target excluded), with9 actual new tests. Strict
Task Contract lifecycle passes7/7: exact28-path boundary and six selector families
running2/2/1/2/1/1 actual tests; each selector's96 zero-match targets are excluded.
Warnings-denied all-target Matrix Clippy and fmt/diff checks pass. Original
Clippy output is retained: four nested-condition style findings and duplicate
private helper module inclusion, resolved with equivalent let-chain conditions
and a scoped test-only helper annotation. No suppression applies to production
code. The final formatted source was followed by a complete package rerun.

The lifecycle reports warnings for mapped project scenarios outside this task;
they remain unqualified, including executable/client owner-approval completion.
No actual Windows/Linux run or new live/hosted job is claimed by this checkpoint.
The external cache handoff and manifest record exact source and evidence hashes.
