# codex app-server --help — transport options excerpt
#
# PIN: this excerpt must be captured from Codex CLI 0.153.4 (the version the
# wire-protocol spec pins, specs/task-rust-codex-protocol.spec.md:18).
# ADR-139 relies on: (a) stdio is the DEFAULT transport (`--listen` default
# `stdio://`), and (b) `--stdio` is an exact synonym ("equivalent to
# `--listen stdio://`") — so a bare `app-server` argv yields stdio.
#
# CAVEAT (recorded honestly): the excerpt below was captured from the local
# Homebrew codex 0.154.0 because 0.153.4 is not installed on this host.
# It is committed as the SHAPE of the capture and the assertions to re-verify;
# `native_codex_stdio_flag_matches_pinned_cli` must FAIL until this file is
# re-captured from a real 0.153.4 binary (version string below then reads
# 0.153.4) or the spec's pin moves to the version actually captured.
#
# captured_version: 0.154.0  (target: 0.153.4 — re-capture required)
# capture_command: codex app-server --help
# ----------------------------------------------------------------------

      --listen <URL>
          Transport endpoint URL. Supported values: `stdio://` (default), `unix://`, `unix://PATH`,
          `ws://IP:PORT`, `off`

          [default: stdio://]

      --stdio
          Use stdio as the transport (equivalent to `--listen stdio://`)
